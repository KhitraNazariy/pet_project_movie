import React, {FC} from 'react';

import scss from './Footer.module.scss';

const Footer: FC = () => {
    return (
        <div className={scss.footer}>
            Footer
        </div>
    );
};

export {Footer};